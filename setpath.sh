# source this file i.e. run it using "source setpath.sh" to make sure that stm-cli and gen.sh are in the path.
# change your .zshrc or .bashrc accordingly as well
export PATH=$PATH:$PWD/app-gen/bin:$PWD/stmcli/bin
